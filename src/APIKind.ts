/**
 * Defines possible values for API metadata object "kind" property
 * @public
 */
export enum APIKind {
  CLASS = "class",
  INTERFACE = "interface",
  ENUM = "enum",
  TYPE_ALIAS = "type-alias"
}